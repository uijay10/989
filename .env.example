PORT=5000
BASE_PATH=/
API_PORT=8080

# Optional app-side DeepSeek estimated USD cap per UTC hour (independent of DeepSeek console).
# Omit or empty = no app hourly cap (official billing only). Set a positive number to throttle (e.g. 0.05).
# DEEPSEEK_HOURLY_BUDGET_USD=0.05

PORT=5000
BASE_PATH=/
API_PORT=8080

# App-side DeepSeek estimated $ / UTC hour (independent of DeepSeek console). Default 0.05 if unset. Set 0 = disable app cap; rely on official billing only.
# DEEPSEEK_HOURLY_BUDGET_USD=0
